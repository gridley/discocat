#include "lsquadrature.h"

// If no template specializations match, an error should be thrown on compile
// (figure this out later)
template <unsigned n, MomType m>
LSQuadrature<n, m>::LSQuadrature()
{
}
